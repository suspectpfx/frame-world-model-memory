const mcRows=[...document.querySelectorAll('.mc-row')];document.querySelector('#mc-replay').addEventListener('click',()=>mcRows.forEach(r=>r.querySelectorAll('video').forEach(v=>{v.currentTime=0;v.play().catch(()=>{});})));document.querySelector('#mc-pause').addEventListener('click',()=>mcRows.forEach(r=>r.querySelectorAll('video').forEach(v=>v.pause())));mcRows.forEach(r=>{const v=[...r.querySelectorAll('video')];v[0].addEventListener('timeupdate',()=>{if(!v[0].paused)v.slice(1).forEach(x=>{if(Math.abs(x.currentTime-v[0].currentTime)>.12)x.currentTime=v[0].currentTime;});});});