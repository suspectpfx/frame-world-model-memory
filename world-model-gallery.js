const wmRows=[...document.querySelectorAll('.wm-row')];function wmPlay(row){const vids=[...row.querySelectorAll('video')];vids.forEach(v=>{v.currentTime=0;v.play().catch(()=>{});});}wmRows.forEach(row=>{const vids=[...row.querySelectorAll('video')];row.querySelector('.wm-replay-row').addEventListener('click',()=>wmPlay(row));vids[0].addEventListener('timeupdate',()=>{if(!vids[0].paused)vids.slice(1).forEach(v=>{if(Math.abs(v.currentTime-vids[0].currentTime)>.12)v.currentTime=vids[0].currentTime;});});});document.querySelector('#wm-replay-all').addEventListener('click',()=>wmRows.filter(r=>!r.hidden).forEach(wmPlay));document.querySelector('#wm-pause-all').addEventListener('click',()=>document.querySelectorAll('.wm-cell video').forEach(v=>v.pause()));document.querySelector('#wm-task').addEventListener('change',e=>wmRows.forEach(r=>{r.hidden=e.target.value!=='all'&&r.dataset.suite!==e.target.value;if(r.hidden)r.querySelectorAll('video').forEach(v=>v.pause());}));