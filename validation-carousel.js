document.querySelectorAll('[data-validation-carousel]').forEach(root=>{const slides=[...root.querySelectorAll('[data-validation-slide]')],dots=[...root.querySelectorAll('[data-validation-dot]')],status=root.querySelector('[data-validation-status]');let index=0;const show=i=>{index=(i+slides.length)%slides.length;slides.forEach((s,j)=>s.hidden=j!==index);dots.forEach((d,j)=>d.setAttribute('aria-selected',String(j===index)));status.textContent=`Study ${index+1} / ${slides.length}`;};root.querySelector('[data-validation-prev]').addEventListener('click',()=>show(index-1));root.querySelector('[data-validation-next]').addEventListener('click',()=>show(index+1));dots.forEach((d,i)=>d.addEventListener('click',()=>show(i)));root.addEventListener('keydown',e=>{if(e.key==='ArrowLeft')show(index-1);if(e.key==='ArrowRight')show(index+1);});show(0);});